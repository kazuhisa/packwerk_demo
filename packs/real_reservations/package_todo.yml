# This file contains a list of dependencies that are not part of the long term plan for the
# 'packs/real_reservations' package.
# We should generally work to reduce this list over time.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-todo
---
".":
  "::ApplicationController":
    violations:
    - dependency
    files:
    - packs/real_reservations/app/controllers/real_reservations_controller.rb
  "::ApplicationRecord":
    violations:
    - dependency
    files:
    - packs/real_reservations/app/models/real_reservation.rb
  "::User":
    violations:
    - dependency
    files:
    - packs/real_reservations/app/models/real_reservation.rb
